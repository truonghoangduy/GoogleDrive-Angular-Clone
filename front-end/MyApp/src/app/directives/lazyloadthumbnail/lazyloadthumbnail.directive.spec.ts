import { LazyloadthumbnailDirective } from './lazyloadthumbnail.directive';

describe('LazyloadthumbnailDirective', () => {
  it('should create an instance', () => {
    const directive = new LazyloadthumbnailDirective();
    expect(directive).toBeTruthy();
  });
});
