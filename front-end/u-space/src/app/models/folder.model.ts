export interface Folder{
    name:string,
    files: Array<File>;
    volume:string,
    createDate:string,
    owner:string,

    // PATH Implentation will be update
}
export interface Section {
  name: string;

  }
