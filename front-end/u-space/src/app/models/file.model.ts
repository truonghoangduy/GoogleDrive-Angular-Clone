export interface File {
    name: string,
    pictureURL: string,
    volume: string,
    createDate: string,
    owner: string,

    // PATH Implentation will be update
}
export interface Section {
  name: string;
}
